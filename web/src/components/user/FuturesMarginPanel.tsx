// Removed: FuturesMarginPanel had zero consumers after the bot detail page refactor.
// Futures positions + margin history now render inline at the consumer site via
// useSupabaseFuturesPositions / useSupabaseMarginHistory (those hooks stay — used elsewhere).

